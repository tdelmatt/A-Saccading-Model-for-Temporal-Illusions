TASK:
    NAME: 'CIFAR'
MODEL:
    SACCADE: 'constant' # apparently not relevant
    FOV_FALLOFF: 0.0 # make it easier -- apparently not relevant
    OBJECTIVES: ['autoencode'] # apparently not relevant (and next step doesn't seem much worse)
    INCLUDE_PROPRIO: False
TRAIN:
    EPOCHS: 30